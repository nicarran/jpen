package jpen.bridge;

public class JPEN_VERSION {
	// BUILD NOTE:
	// The tokens ${VERSION} and ${DIST_NAME} are replaced at build with their
	// respective values
	public static final String DIST_REVISION = "${VERSION}";
	public static final String DIST_NAME = "${DIST_NAME}";
	
	private JPEN_VERSION() {
	}
}
