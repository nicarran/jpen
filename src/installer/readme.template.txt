JPen Extension Installer

This installer will install JPen (${VERSION}) as an extension in your Java environment.

When the install is successful, you will be able to use JPen pressure and tilt sensing from any Java application.
